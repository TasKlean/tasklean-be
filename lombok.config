# Root of the Lombok config
config.stopBubbling = true

# Add @lombok.Generated to generated members so coverage tools (JaCoCo) skip
# boilerplate getters/setters/builders/equals/hashCode.
lombok.addLombokGeneratedAnnotation = true
